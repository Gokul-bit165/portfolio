[build]
  command = "npm install --include=dev && node ./node_modules/vite/bin/vite.js build"
  publish = "dist"

[build.environment]
  NODE_ENV = "development"
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"
